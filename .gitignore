# Локальные настройки машины разработчика (пути Windows, персональные permissions)
.claude/settings.local.json

# Операционные доки с инфраструктурными деталями (SSH-доступ, Funnel-URL, имена сервисов/БД).
# Репозиторий публичный и отдаётся через Pages — эти файлы НЕ должны попасть наружу.
# Живут локально и на самой ВМ в /opt/bc-assistant/.
CLAUDE.md
HANDOFF.md

# Прочее
.DS_Store
Thumbs.db
